// Eqwal Ability — small progressive enhancements.
// Fades the page content in once it has loaded, avoiding a layout flash.
document.addEventListener('DOMContentLoaded', () => {
  document.body.classList.add('is-ready');
});
